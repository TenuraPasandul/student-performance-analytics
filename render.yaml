services:
  - type: web
    name: student-performance-bi
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
